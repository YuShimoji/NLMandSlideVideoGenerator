# Handover: 2026-03-17 SP-033 Phase 2 E2E完走 + パイプライン実用化

## 完了事項

### Geminiモデルフォールバックチェーン
- `gemini-2.5-flash` (高品質, 20req/day) → `gemini-2.0-flash` (高クォータ) → モック
- `GEMINI_MODEL` 環境変数で主モデル切替可
- E2E確認: 両モデルクォータ超過時もモック経由で全ステップ完走

### Speaker伝搬修正
- `script_alignment.py`: analysis items に `speaker` フィールドを伝搬
- `export_to_csv`: speaker がセグメントから正しく取得される
- `speaker_mapping` 動作確認: Host1→れいむ, Host2→まりさ

### モック品質改善
- Host1/Host2対話形式 (YMM4 speaker_mapping互換)
- トピック反映型: プロンプトからトピック名を抽出し、全セグメントに反映
- `_build_mock_content()` を独立メソッドに分離

### クレジットファイル自動生成
- `image_credits.txt` をパイプライン完了時に自動生成
- `VisualResourceOrchestrator.last_stock_images` でダウンロード画像を追跡
- `StockImageClient.get_attribution()` でフォトグラファー名付きクレジット出力

### AIImageProvider基盤 (SP-033 Phase 3)
- `src/core/visual/ai_image_provider.py` 新規作成 (262行)
- Orchestratorに `ai_provider` パラメータ追加
- stock取得失敗セグメントへのAI画像フォールバック経路確保
- テスト15件PASS

### resumeバグ修正
- `"vis_package" not in dir()` → `vis_package is None` に修正
- resume/skip時のOrchestrator再構築が安全に動作

### TextSlideGenerator (SP-033 Phase 3b)
- `src/core/visual/text_slide_generator.py` (323行)
- Orchestratorに `work_dir` パラメータ追加
- source="none" セグメントを自動テキストスライドで埋める

### BGMテンプレート自動配置 (SP-031)
- `config/style_template*.json` に `bgm` セクション追加 (volume_percent, fade_in/out, layer)
- Python `StyleTemplate.bgm` フィールド + ビルトインデフォルト
- C# `StyleTemplateLoader.BgmConfig` + `CsvImportDialog` テンプレート読み込み
- fadeIn/fadeOut 分離対応 (従来は同一値)

### CLI UX改善
- `--help` がトップレベルで正しく表示されるよう修正
- pipeline サブコマンドのhelp文字列を英語化 (Windows cp1252文字化け対策)
- `templates` 一覧にBGM音量表示追加
- `slide_paths` → `slide_image_paths` keyword引数修正

### ドキュメント整備
- INDEX.md: Shift-JIS→UTF-8 文字化け修正、全セクション再構成
- spec-index.json: SP-031 pct=100完了
- video_quality_pipeline_spec.md: Phase 0-4全完了ステータス
- backlog.md: ロードマップ更新

## テスト状況
- Python: 330件PASS (前回301件)
- E2Eパイプライン: 全6ステップ完走確認 (collect→script→align→review→orchestrate→assemble)
- Pexels API: 実ダウンロード確認済み (407KB + 152KB)
- BGMテンプレート: 7テスト追加 (読み込み/保存/バリアント/実テンプレート)

## 次回推奨

### 高優先度
1. **Gemini APIクォータリセット後の実コンテンツ生成テスト**
   - クォータリセット (24h) 後に実トピックでフルパイプライン実行
   - 実Gemini出力のセグメント品質を確認
2. **YMM4実機テスト** (バッチ)
   - ストック画像付きCSVインポート
   - BGMテンプレート設定の実機確認
   - 7種アニメーション確認
   - 字幕テンプレート確認 (SP-030)

### 中優先度
3. **Gemini有料プラン検討** — 無料枠 (20req/day) は本番運用に不足
4. **AI画像生成実接続** — AIImageProvider + Gemini Imagen API実呼び出し
5. **テンプレートバリアント実運用** — ユーザーカスタムテンプレートの作成・配布フロー

## コミット履歴 (本セッション全体)
- `6299c46` docs: Geminiモデルフォールバック決定 + E2E完走確認反映
- `6b4ed61` fix: speaker伝搬修正 + モック対話形式化 + AIImageProvider基盤
- `4b2a11f` feat: パイプラインにクレジットファイル自動生成を追加
- `85ec75e` feat: モック台本をトピック反映型に改善
- `2342059` feat: TextSlideGenerator + orchestrator統合 + keyword引数修正
- `c40aaf3` feat: SP-031 BGMテンプレート自動配置 (Python + C# 双方)
- `89c033b` docs: SP-031完了反映 + CLAUDE.md状態更新
- `f695bc2` docs: INDEX.md文字化け修正 + spec-index SP-031完了反映
- `960f9a7` docs: Phase 4 BGM完了反映 + ロードマップ更新
- `69413e0` fix: CLI --help修正 (トップレベル表示 + 日本語文字化け解消)
- `5d47b25` feat: templates一覧にBGM音量表示を追加

## パイプライン実行例
```bash
cd "c:/Users/thank/Storage/Media Contents Projects/NLMandSlideVideoGenerator"
./venv/Scripts/python.exe scripts/research_cli.py pipeline \
  --topic "量子コンピュータの最新動向" \
  --auto-images \
  --duration 5
```

## 出力ファイル構成
```
data/research/rp_YYYYMMDD_HHMMSS/
  package.json           — 収集した資料パッケージ
  generated_script.json  — 生成された台本
  alignment_report.json  — 台本と資料の照合結果
  final.csv              — レビュー後の話者+テキストCSV
  timeline.csv           — YMM4インポート用4列CSV (speaker, text, image_path, animation)
  image_credits.txt      — ストック画像のクレジット情報
  pipeline_state.json    — ステップ別進捗状態 (resume用)
  stock_images/          — ダウンロードされたストック画像
```
