[pytest]
norecursedirs = scripts .git .shared-workflows venv node_modules
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: 外部依存やE2E寄りの統合テスト（普段は除外して実行）
    slow: 実行に時間がかかるテスト
