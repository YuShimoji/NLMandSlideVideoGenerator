# image_slide サンプル

CSV 3列目に画像パスを指定するスライド動画用サンプル。

## CSVフォーマット

```
話者,テキスト,画像パス（省略可）
```

- 3列目は省略可能（後方互換）
- 画像パスは絶対パスを指定
- 画像ファイルが存在しない場合はWarningを記録しスキップ

## 使い方

1. `timeline.csv` の画像パスを実際のファイルパスに書き換える
2. YMM4 で NLMSlidePlugin > CSVインポート
3. ImageItem がタイムラインに配置される
